apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Release.Name }}-architecture-service-sa
  annotations:
    azure.workload.identity/client-id: {{ .Values.keyvault.clientId | quote }}
  labels:
    app: architecture-service
