apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: archgen-prod-auth-service-hpa
  labels:
    app: auth-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: archgen-prod-auth-service
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80
