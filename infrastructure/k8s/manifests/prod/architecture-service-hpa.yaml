apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: archgen-prod-architecture-service-hpa
  labels:
    app: architecture-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: archgen-prod-architecture-service
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80
