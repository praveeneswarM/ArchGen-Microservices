apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: archgen-prod-project-service-hpa
  labels:
    app: project-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: archgen-prod-project-service
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80
