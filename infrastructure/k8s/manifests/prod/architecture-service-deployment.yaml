apiVersion: apps/v1
kind: Deployment
metadata:
  name: archgen-prod-architecture-service
  labels:
    app: architecture-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: architecture-service
  template:
    metadata:
      labels:
        app: architecture-service
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: archgen-prod-architecture-service-sa
      containers:
        - name: architecture-service
          image: "acrarchgenprod.azurecr.io/architecture-service:latest"
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8003
              protocol: TCP
          volumeMounts:
            - name: secrets-store-inline
              mountPath: "/mnt/secrets-store"
              readOnly: true
          resources:
            limits:
              cpu: 500m
              memory: 1Gi
            requests:
              cpu: 250m
              memory: 512Mi
      volumes:
        - name: secrets-store-inline
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: "archgen-prod-architecture-keyvault-provider"
