apiVersion: v1
kind: Service
metadata:
  name: archgen-dev-project-service
  labels:
    app: project-service
spec:
  type: ClusterIP
  ports:
    - port: 8002
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: project-service
