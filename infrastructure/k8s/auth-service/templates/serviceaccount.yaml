apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Release.Name }}-auth-service-sa
  annotations:
    azure.workload.identity/client-id: {{ .Values.keyvault.clientId | quote }}
  labels:
    app: auth-service
