apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-architecture-service
  labels:
    app: architecture-service
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: architecture-service
